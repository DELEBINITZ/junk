"""Guardrail spine (plan §10). Input + output pipelines with heuristic defaults
(regex PII, prompt-injection) and config-gated model adapters (Presidio,
Prompt Guard 2, Llama Guard 3) that lazy-load and fall back to heuristics."""
