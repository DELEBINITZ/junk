"""Platform CORE — the frozen chassis.

Nothing in this package names a specific feature. Capability MODULES live under
`app/capabilities/` and depend on `app.core.contracts`; the dependency never
points the other way. See PRODUCTION_AGENTIC_PLAN.txt sections 4-6.
"""
